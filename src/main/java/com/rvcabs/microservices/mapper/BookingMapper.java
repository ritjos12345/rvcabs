package com.rvcabs.microservices.mapper;

public class BookingMapper {
	/*
	 * public static CarDetailDto mapToDto(CarDetailsEntity carDetailsEntity) { //
	 * List<CarDetailDto> carDetailDtos = new ArrayList<CarDetailDto>(); // if (null
	 * != masterDataDtos && masterDataDtos.size() > 0) { // for (CarDetail
	 * driverContactNum : masterDataDtos) { CarDetailDto carDetailDto = new
	 * CarDetailDto(); }
	 */
}
